{
  "api_url": "http://127.0.0.1:38147",
  "api_url_resolver": "",
  "_help": "Set api_url to a direct backend, or leave it empty and set api_url_resolver to a stable HTTPS JSON/text/meta-refresh pointer that resolves to the current backend. Remote backends must use HTTPS; local HTTP is allowed only on loopback."
}
